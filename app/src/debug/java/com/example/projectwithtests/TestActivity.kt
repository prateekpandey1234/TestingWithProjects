package com.example.projectwithtests

import androidx.appcompat.app.AppCompatActivity
import dagger.hilt.android.AndroidEntryPoint
/**We also need to create a new separate manifest file for our this new fake activity ...
 * just copy paste and edit the main manifest file in this directory
 * */
@AndroidEntryPoint
class TestActivity : AppCompatActivity() {
}